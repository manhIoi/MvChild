import React from 'react';
import {View, Text} from 'react-native';
import {headerDimensions} from '../constants/dimensions';

const Footer = () => {
  return <View style={{height: headerDimensions.fullHeight}} />;
};

export default Footer;
