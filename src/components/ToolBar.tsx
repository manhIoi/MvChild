import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const ToolBar = () => {
  return (
    <View>
      <Text>this is tool bar</Text>
    </View>
  );
};

export default ToolBar;

const styles = StyleSheet.create({});
